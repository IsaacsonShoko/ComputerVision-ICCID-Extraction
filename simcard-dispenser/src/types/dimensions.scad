// Dimensions of SIM card types
// Units: mm

// Full-Size SIM (1FF)
full_size_sim = [85.6, 53.98, 0.76]; // Length, Width, Thickness

// Mini-SIM (2FF)
mini_sim = [25.0, 15.0, 0.76]; // Length, Width, Thickness

// Micro-SIM (3FF)
micro_sim = [15.0, 12.0, 0.76]; // Length, Width, Thickness

// Nano-SIM (4FF)
nano_sim = [12.3, 8.8, 0.67]; // Length, Width, Thickness

// Embedded SIM (MFF2)
embedded_sim = [6.0, 5.0, 1.0]; // Length, Width, Thickness (approximate)